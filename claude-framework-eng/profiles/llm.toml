name = "llm"
description = "Systems in which a language model produces text, decisions or actions that the code uses."
critical_surface = "The model's behaviour is not in the code: a prompt that holds on three examples says nothing about the rest, and without repeatable evaluation every change is an opinion."

agents = ["debugger", "api-scout", "scientific-reviewer", "results-analyst", "silent-failure-hunter", "conversation-analyzer", "comment-analyzer"]
cycles = ["research"]

shared = [
  "core/conventions.md",
  "core/coding-standards.md",
  "core/architecture-guide.md",
  "core/testing-guide.md",
  "core/debugging-playbook.md",
  "core/review-checklist.md",
  "core/security-guide.md",
  "domain/research-principles.md",
  "domain/llm-guide.md",
]

[settings]
# Reply style: it governs only what the user reads.
outputStyle = "Reporting"

[settings.permissions]
deny = [
  "Read(./**/.env)", "Read(./**/.env.*)", "Read(./**/*.env)",
  "Read(./**/*.key)", "Read(./**/*.pem)", "Read(./**/*.p12)",
]
